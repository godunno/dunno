<% # TODO: Move this to scss file. %>
<% colors = SHARED_CONFIG["v1"]["courses"]["schemes"] %>
<% colors.each_with_index do |scheme, index| %>
  .scheme-<%= index + 1%> {
    border-color: $light-grey-color;
    background-color: <%= scheme %>;
  }
<% end %>
